services:
  database:
    image: mariadb:latest
    container_name: dnd_database
    volumes:
      - data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: demo
      MYSQL_USER: user
      MYSQL_PASSWORD: pass
    ports:
      - 3309:3306
volumes:
  data: 